import React from "react";

interface blogProps {}

const blog: React.FC<blogProps> = ({}) => {
  return (
    <div className="w-full">
      <p className="text-center">Nothing noteworthy yet...</p>
    </div>
  );
};

export default blog;
